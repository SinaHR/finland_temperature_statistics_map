# Pinned dependencies for the Finland climate probability dashboard preprocessing.
# Python 3.10. Install into a venv: python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
netCDF4==1.7.4
numpy==2.2.6
pyproj==3.7.1
requests==2.34.2
xarray==2025.6.1
